using System.Numerics;
using UnityEngine;
using Vector3 = UnityEngine.Vector3;

public class EnemyTest : MonoBehaviour
{
    // [Task]
    // bool playerInSight() => Vector3.Distance(transform.position, target.position) < 5.0f;
}
